spring:
  cloud:
    openfeign:
      client:
        config:
          calendar:
            url: http://localhost:9092/api/v1/calendar
          photo:
            url: http://localhost:9092/api/v1/calendar/photos